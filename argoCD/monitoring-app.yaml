apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: kube-prometheus-stack
  namespace: argocd
spec:
  project: default
  source:
    chart: kube-prometheus-stack
    repoURL: https://prometheus-community.github.io/helm-charts
    targetRevision: 61.3.0
    helm:
      valueFiles:
        - ../monitoring/prometheus/values.yaml # Agar values file local repo me h to iska path map hoga, ya abhi bina valuesFiles ke bhi check kar sakte ho.
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true # <-- Sahi tarika ye hai! C bada hoga aur list me aayega.