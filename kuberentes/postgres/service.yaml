apiVersion: v1
kind: Service
metadata:
  name: postgres-service
spec:
  clusterIP: None # 👑 Headless service bana rahe hain databases ke liye
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432